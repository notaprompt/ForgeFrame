{
  "name": "forgeframe",
  "private": true,
  "description": "Local intelligence infrastructure -- routing, memory, provenance",
  "homepage": "https://campos.works/projects/forgeframe",
  "author": "Alexander Campos <adc.acampos@gmail.com> (https://campos.works)",
  "repository": {
    "type": "git",
    "url": "https://github.com/notaprompt/ForgeFrame.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces --if-present",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
