{
  "name": "forgeframe",
  "private": true,
  "description": "Sovereign AI middleware — routing, memory, provenance",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
